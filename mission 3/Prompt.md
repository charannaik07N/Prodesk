Create a website that includes proper error handling for the API call and spinner reload function.

The main issues to address are:

- The catch function throws `Uncaught ReferenceError: error is not defined`.
- The spinner reload function should show a loading spinner instead of plain text.
- In battle mode, the winner should be properly declared and defined before it is used.

Focus mostly on error handling, fixing the undefined `error` variable, and ensuring the `winner` variable is declared correctly.
